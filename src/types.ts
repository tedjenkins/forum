// * ---------------------------------------
// * Application-wide types definition file.
// * ---------------------------------------

// For use when creating form components
export type formEls = Array<{ id?: string, html: string }>;
